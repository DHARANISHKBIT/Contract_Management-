# Public API origin (no trailing slash). Required when the frontend is not served from the same host as the API.
# Example after deploying backend: https://contract-api.onrender.com
VITE_API_BASE_URL=http://localhost:5000

# Optional: Google Form prefill for admin user onboarding (see admin users page)
# VITE_USER_DETAILS_FORM_URL=
# VITE_USER_DETAILS_EMAIL_ENTRY=
